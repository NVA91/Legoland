# Admin panel token – generate with: openssl rand -base64 48
VAULTWARDEN_ADMIN_TOKEN=PLEASE_CHANGE_THIS_TOKEN

# Allow new user sign-ups (set to true during initial setup, then false)
VAULTWARDEN_SIGNUPS_ALLOWED=false
